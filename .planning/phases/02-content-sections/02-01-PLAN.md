---
phase: 02-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/content.ts
  - src/components/layout/Header.tsx
  - src/app/globals.css
autonomous: true
requirements: [CONT-09, TECH-06]

must_haves:
  truths:
    - "All case study content is defined in TypeScript with type-safe interfaces, sourced from toolkit master file data"
    - "Navigation links in the sticky header smooth-scroll to the correct sections with proper offset for the sticky header"
    - "Clicking a nav link scrolls the page without the target section hiding behind the header"
  artifacts:
    - path: "src/data/content.ts"
      provides: "All section content as typed constants -- case studies, impact metrics, What I Do categories, personal section drafts, nav links, hero content, contact info"
      exports: ["CaseStudy", "ImpactMetric", "NavLink", "WhatIDoCategory", "caseStudies", "impactMetrics", "navLinks", "whatIDoCategories", "heroContent", "beyondContent", "standForContent", "rightFitContent", "contactInfo"]
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky header with navigation links using plain anchor tags"
      contains: "navLinks.map"
    - path: "src/app/globals.css"
      provides: "Smooth scroll behavior and sticky header offset"
      contains: "scroll-behavior: smooth"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/data/content.ts"
      via: "import { navLinks }"
      pattern: "import.*navLinks.*from.*content"
    - from: "src/app/globals.css"
      to: "html element"
      via: "scroll-padding-top: var(--header-height)"
      pattern: "scroll-padding-top"
---

<objective>
Create the content data layer and navigation infrastructure for all 8 portfolio sections.

Purpose: Establish typed content constants sourced from the job search toolkit master case study files, and wire smooth-scroll navigation into the existing sticky header so section components in Plans 02 and 03 can import content and be navigable immediately.

Output: `src/data/content.ts` with all section content as typed TypeScript constants, updated Header with nav links, and CSS smooth scroll declarations.
</objective>

<execution_context>
@/Users/marieanik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marieanik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/components/layout/PageContainer.tsx:
```typescript
interface PageContainerProps {
  children: ReactNode;
  className?: string;
  as?: ElementType;
}
export function PageContainer({ children, className, as: Component = "div" }: PageContainerProps)
```

From src/components/layout/Header.tsx:
```typescript
export function Header()
// Currently has two empty <div /> placeholders:
// {/* Logo / name placeholder -- Phase 2 */}
// {/* Nav + theme toggle placeholder -- Phase 2/3 */}
```

From src/styles/tokens.css (relevant tokens):
```css
--header-height: clamp(3.5rem, 3.25rem + 0.5vw, 4rem);
--space-section: clamp(4rem, 3rem + 2.5vw, 6rem);
--text-display / --text-h2 / --text-h3 / --text-body / --text-sm / --text-lg
--accent-primary (blue-600 light, cyan-400 dark)
--text-primary / --text-secondary / --text-on-accent
--bg-primary / --bg-secondary / --bg-surface
```
</interfaces>

Source content files (read these for case study data):
@~/Desktop/job-search-toolkit/master-files/case-studies/epic-submit.md
@~/Desktop/job-search-toolkit/master-files/case-studies/epic-engage.md
@~/Desktop/job-search-toolkit/master-files/case-studies/ptmp.md
@~/Desktop/job-search-toolkit/master-files/case-studies/bambora.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create typed content data layer with all section content</name>
  <files>src/data/content.ts</files>
  <action>
Create `src/data/content.ts` with TypeScript interfaces and `as const` data for ALL 8 sections. This is the single source of truth for all portfolio content.

**Interfaces to define:**
- `CaseStudy` — id, title (outcome-first headline), metric (the big number), metricLabel, role, hook (1-2 sentence strategic summary)
- `ImpactMetric` — value (big number string), label (short name), context (one-line description)
- `NavLink` — label, href (anchor like "#work")
- `WhatIDoCategory` — title, description
- `HeroContent` — name, title, subtitle
- `ContactInfo` — email, linkedinUrl, linkedinLabel

**Case study data (4 studies, from toolkit master files -- DO NOT include VIP):**
1. EPIC.submit: title="One Architecture, Many Teams: A Platform That Scales Without Redesign", metric="100%", metricLabel="Centralized in 5 months", role="Senior UX Designer & Product Lead", hook="Built a modular document submission system starting with the hardest use case, then watched it expand across an entire organization without a single structural change."
2. EPIC.engage: title="From 5% Useful to 80% Relevant: Redesigning Public Engagement for 5M Residents", metric="5% to 80%", metricLabel="Comment quality improvement", role="Senior UX/Product Designer, Researcher, Product Owner", hook="Constraint-driven design transformed the quality of community feedback and built an admin system that's run itself for over two years."
3. NSC Interactions (PTMP): title="Cross-System Intelligence for 20,000+ Carriers: From Months to Hours", metric="$390-495M", metricLabel="Annual social value", role="Senior UX/Product Designer & Information Architect", hook="Designed the data architecture that linked four isolated enterprise systems, uncovering fraud patterns that previously took months to detect."
4. Bambora SSO: title="Migrating 20,000+ Merchants to Modern Auth Without Breaking Payments", metric="20,000+", metricLabel="Merchants migrated", role="UX Designer", hook="Designed five distinct authentication flows within severe technical constraints and zero tolerance for downtime."

**Impact metrics (6 cross-project aggregate):**
1. value="$390-495M", label="Annual Social Value", context="Public safety impact from cross-system fraud detection"
2. value="5% to 80%", label="Comment Quality", context="Transformed public engagement from noise to actionable input"
3. value="8,000+", label="Staff Hours Saved", context="Annual operational time recovered through system integration"
4. value="700-1,000", label="Crashes Prevented", context="Annual road safety impact from data-driven enforcement"
5. value="98%", label="Task Success Rate", context="Achieved across diverse users including older adults in rural areas"
6. value="100%", label="Centralized in 5 Months", context="Full migration of document submissions to unified platform"

**What I Do categories (4):**
1. "UX & Product Design" — "End-to-end product ownership from discovery through production, for complex multi-stakeholder systems"
2. "Design Systems" — "Modular architectures that scale across teams without redesign"
3. "AI Workflows" — "AI-enhanced research and analysis integrated into design practice"
4. "Research & Strategy" — "Systematic stakeholder research programs driving long-term product direction"

**Hero content:**
- name: "Marie Anik Paradis"
- title: "Senior UX & Product Designer"
- subtitle: "Crafting thoughtful digital experiences that bridge user needs with business goals." (or similar -- keep concise)

**Personal section content:**
- `beyondContent`: object with `title` and `body` as placeholder string: "This section is coming soon -- Marie will provide personal content that reflects who she is beyond the portfolio." Make the placeholder clearly marked as TODO so it's obvious this needs user-provided content.
- `standForContent`: object with `title` ("What I Stand For") and `values` array of {heading, description}. Draft 4 values from toolkit themes: (1) "Design for the most constrained users first", (2) "Architecture decisions matter more than UI polish", (3) "Systems thinking over screen-level thinking", (4) "Operational independence: design things that work without the designer". Each value gets a 1-2 sentence description expanding the principle.
- `rightFitContent`: object with `title` ("The Right Fit") and structured data: `roles` (Staff Product Designer, Design Lead), `teams` (Platform teams, B2B SaaS, government digital services), `problems` (Complex multi-stakeholder systems, cross-team coordination, 0-to-1 product definition), `scale` ("Enterprise systems serving thousands to millions of users"). Add a `summary` string that opens the section.

**Contact info:**
- email: "hello@marieanik.com" with a TODO comment noting this needs verification
- linkedinUrl: "https://linkedin.com/in/marieanik" with a TODO comment noting this needs verification
- linkedinLabel: "Connect on LinkedIn"

**Nav links (5 links per research recommendation):**
1. label="Work", href="#work"
2. label="Impact", href="#impact"
3. label="About", href="#about"
4. label="Fit", href="#fit"
5. label="Contact", href="#contact"

Use `as const` assertions on arrays for maximum type safety. Export all interfaces and data constants as named exports.
  </action>
  <verify>
    <automated>cd /Users/marieanik/Desktop/portfolio && npx tsc --noEmit src/data/content.ts 2>&1 | head -20</automated>
  </verify>
  <done>src/data/content.ts exists with exported TypeScript interfaces (CaseStudy, ImpactMetric, NavLink, etc.) and all content constants populated with real data from toolkit master files. File compiles without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire navigation links into Header and add smooth scroll CSS</name>
  <files>src/components/layout/Header.tsx, src/app/globals.css</files>
  <action>
**Update src/app/globals.css:**
Add to the existing CSS (do not remove existing content):
```css
html {
  scroll-behavior: smooth;
  scroll-padding-top: var(--header-height);
}
```
This enables CSS-only smooth scrolling with proper offset for the sticky header. Do NOT use JavaScript scrollIntoView -- the CSS approach is simpler, respects prefers-reduced-motion automatically, and requires no client component.

**Update src/components/layout/Header.tsx:**
Replace the two empty `<div />` placeholders with:

1. **Logo/name slot (left side):** Display "Marie Anik Paradis" as a plain `<a href="#top">` link (or just text) styled with `--text-body` size, `--font-semibold` weight, `--text-primary` color. Keep it simple -- just the name, no logo image.

2. **Navigation (right side):** Import `navLinks` from `@/data/content`. Map over `navLinks` to render plain `<a href={link.href}>` elements (NOT Next.js Link -- per research anti-pattern guidance, Link adds client-side routing overhead for same-page anchors). Style each link with `--text-sm` font size, `--text-secondary` color, no text-decoration. Use flex layout with `--space-6` gap between links.

The Header MUST remain a Server Component (no "use client" directive). Plain anchor tags need no client-side JavaScript.

Do NOT add the theme toggle -- that's Phase 3.

Hide nav links on mobile for now (use Tailwind `hidden md:flex` on the nav container). Mobile nav is not in scope for this phase.
  </action>
  <verify>
    <automated>cd /Users/marieanik/Desktop/portfolio && npx tsc --noEmit && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Header displays "Marie Anik Paradis" on the left and 5 nav links (Work, Impact, About, Fit, Contact) on the right. globals.css includes scroll-behavior:smooth and scroll-padding-top. Build succeeds without errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no type errors in content.ts or Header.tsx)
2. `npm run build` succeeds
3. content.ts exports all expected interfaces and data constants
4. Header.tsx imports navLinks and renders anchor tags with correct hrefs
5. globals.css contains `scroll-behavior: smooth` and `scroll-padding-top: var(--header-height)`
</verification>

<success_criteria>
- src/data/content.ts exists with typed interfaces and real content data for all 8 sections
- Header shows name and 5 nav links using plain anchor tags
- CSS smooth scroll is declared globally with sticky header offset
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-01-SUMMARY.md`
</output>
