---
phase: 02-content-sections
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/Hero.tsx
  - src/components/sections/WhatIDo.tsx
  - src/components/sections/ImpactMetrics.tsx
  - src/components/sections/CaseStudyCard.tsx
  - src/components/sections/CaseStudies.tsx
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04]

must_haves:
  truths:
    - "Hero section displays Marie's name as H1 in display-sized typography, role title, and experience summary"
    - "Impact metrics section shows 6 real metrics as large accent-colored numbers with labels and context in a 3x2 grid"
    - "What I Do section breaks work into 4 specialty categories with descriptions"
    - "Case study section displays 4 cards in a vertical stack with outcome-first titles, key metrics, roles, and strategic hooks"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      provides: "Hero section with H1 name, role title, subtitle"
      contains: "heroContent"
    - path: "src/components/sections/WhatIDo.tsx"
      provides: "Four specialty categories in responsive grid"
      contains: "whatIDoCategories"
    - path: "src/components/sections/ImpactMetrics.tsx"
      provides: "6 impact metrics in 3x2 grid with accent-colored numbers"
      contains: "impactMetrics"
    - path: "src/components/sections/CaseStudyCard.tsx"
      provides: "Individual case study card component"
      contains: "CaseStudy"
    - path: "src/components/sections/CaseStudies.tsx"
      provides: "Vertical stack of case study cards"
      contains: "caseStudies"
  key_links:
    - from: "src/components/sections/Hero.tsx"
      to: "src/data/content.ts"
      via: "import { heroContent }"
      pattern: "import.*heroContent.*from.*content"
    - from: "src/components/sections/ImpactMetrics.tsx"
      to: "src/data/content.ts"
      via: "import { impactMetrics }"
      pattern: "import.*impactMetrics.*from.*content"
    - from: "src/components/sections/CaseStudies.tsx"
      to: "src/data/content.ts"
      via: "import { caseStudies }"
      pattern: "import.*caseStudies.*from.*content"
---

<objective>
Build the upper showcase section components: Hero, What I Do, Impact Metrics, and Case Studies.

Purpose: These are the "hook and prove it" sections of the editorial narrative arc. They display Marie's identity, specialties, quantitative impact, and detailed case study summaries using real data from the toolkit master files.

Output: 5 Server Components in `src/components/sections/` that render real content from `src/data/content.ts`.
</objective>

<execution_context>
@/Users/marieanik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marieanik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output: content.ts interfaces -->

From src/data/content.ts (created in Plan 01):
```typescript
export interface CaseStudy {
  id: string;
  title: string;        // Outcome-first headline
  metric: string;       // Key number, e.g. "5% to 80%"
  metricLabel: string;  // What the metric measures
  role: string;         // Role title
  hook: string;         // 1-2 sentence strategic hook
}

export interface ImpactMetric {
  value: string;        // The big number, e.g. "$390-495M"
  label: string;        // Short label
  context: string;      // One-line description
}

export interface WhatIDoCategory {
  title: string;
  description: string;
}

// HeroContent interface with name, title, subtitle

export const caseStudies: CaseStudy[];
export const impactMetrics: ImpactMetric[];
export const whatIDoCategories: WhatIDoCategory[];
export const heroContent: HeroContent;
```

From src/components/layout/PageContainer.tsx:
```typescript
export function PageContainer({ children, className, as }: PageContainerProps)
// Polymorphic container with responsive padding, max-width 1200px
```

Established patterns (from Phase 1):
- Inline styles with CSS variable references: `style={{ fontSize: "var(--text-h2)" }}`
- Alternating backgrounds: odd sections = --bg-primary, even = --bg-secondary
- Fluid typography: --text-display (H1), --text-h2 (section headings), --text-h3 (sub-items)
- All sections use Server Components (no "use client")
- PageContainer wraps all section inner content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero, WhatIDo, and ImpactMetrics section components</name>
  <files>src/components/sections/Hero.tsx, src/components/sections/WhatIDo.tsx, src/components/sections/ImpactMetrics.tsx</files>
  <action>
Create `src/components/sections/` directory and build three Server Components. All follow the established inline-style + CSS variable pattern from Phase 1. No "use client" directive on any of them.

**Hero.tsx (CONT-01):**
- Import `heroContent` from `@/data/content` and `PageContainer` from `@/components/layout/PageContainer`
- Render a `<section>` with `paddingTop: "var(--space-section)"`, `paddingBottom: "var(--space-section)"` (hero uses --bg-primary, which is default -- no explicit backgroundColor needed)
- Per CONTEXT.md: hero gets spacious treatment with display-sized text
- `<h1>` with `fontSize: "var(--text-display)"`, `lineHeight: "var(--text-display-lh)"`, `letterSpacing: "var(--tracking-tight)"`, `fontWeight: "var(--font-semibold)"`, `color: "var(--text-primary)"` -- render heroContent.name
- Below H1: role title using `--text-h3` size, `--text-secondary` color, with `marginTop: "var(--space-4)"`
- Below role: subtitle using `--text-lg` size, `--text-secondary` color, `maxWidth: "36rem"`, with `marginTop: "var(--space-6)"`
- Do NOT include character illustration slot -- per research open question, build without it for now
- Do NOT add a section `id` attribute -- Hero doesn't need one (it's at the top of the page and has no nav link)

**WhatIDo.tsx (CONT-03):**
- Import `whatIDoCategories` from `@/data/content` and `PageContainer`
- `<section>` with `--bg-secondary` background (alternating pattern: Hero=primary, WhatIDo=secondary)
- Section heading: `<h2>` with `fontSize: "var(--text-h2)"`, standard heading styles, text "What I Do"
- Per CONTEXT.md: WhatIDo doesn't need a nav link or section id (immediately below hero, always visible after scrolling past hero)
- Render `whatIDoCategories` in a responsive grid: `gridTemplateColumns: "repeat(auto-fill, minmax(min(100%, 280px), 1fr))"`, `gap: "var(--space-8)"`
- Each category: `<h3>` with `--text-h3`, `--text-primary`, `--font-semibold` for the category title; `<p>` with `--text-body`, `--text-secondary` for the description
- Keep it clean -- no cards or borders, just a typographic grid

**ImpactMetrics.tsx (CONT-02):**
- Import `impactMetrics` from `@/data/content` and `PageContainer`
- `<section id="impact">` with `--bg-primary` background (alternating: WhatIDo=secondary, Impact=primary)
- Per CONTEXT.md: Impact Metrics gets a larger H2 (use `--text-h2`)
- Section heading: `<h2>` "Impact"
- Render in a 3x2 grid: `gridTemplateColumns: "repeat(auto-fill, minmax(min(100%, 300px), 1fr))"`, `gap: "var(--space-8)"`
- Each metric: big number `<span>` with `fontSize: "var(--text-display)"`, `color: "var(--accent-primary)"` (blue in light, cyan in dark), `fontWeight: "var(--font-semibold)"`; label `<p>` with `--text-lg`, `--font-semibold`, `--text-primary`; context `<p>` with `--text-body`, `--text-secondary`
- Use `--text-secondary` for context description (NOT --text-muted, which fails WCAG AA)
  </action>
  <verify>
    <automated>cd /Users/marieanik/Desktop/portfolio && npx tsc --noEmit src/components/sections/Hero.tsx src/components/sections/WhatIDo.tsx src/components/sections/ImpactMetrics.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Hero.tsx renders name (H1), role, and subtitle from heroContent. WhatIDo.tsx renders 4 specialty categories in a responsive grid. ImpactMetrics.tsx renders 6 metrics with accent-colored numbers in a 3x2 grid. All are Server Components with no "use client". All compile without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create CaseStudyCard and CaseStudies section components</name>
  <files>src/components/sections/CaseStudyCard.tsx, src/components/sections/CaseStudies.tsx</files>
  <action>
**CaseStudyCard.tsx (sub-component for CONT-04):**
- Import `CaseStudy` type from `@/data/content`
- Props: `{ study: CaseStudy }`
- Render a full-width card using the established card pattern from Phase 1:
  ```
  backgroundColor: "var(--bg-surface)"
  border: "1px solid var(--border-default)"
  borderRadius: "var(--radius-lg)"
  boxShadow: "var(--shadow-md), var(--glow-subtle)"
  padding: "var(--space-8)"
  ```
- Inside the card:
  - `<h3>` with `--text-h3`, `--text-primary`, `--font-semibold` -- renders `study.title` (outcome-first headline)
  - Metric display: `<span>` with `--text-h2` size, `--accent-primary` color, `--font-semibold` for `study.metric`; followed by `<span>` with `--text-sm`, `--text-secondary` for `study.metricLabel`. Use flex layout with `alignItems: "baseline"`, `gap: "var(--space-3)"` to place metric and label on the same line
  - Role: `<p>` with `--text-sm`, `--text-secondary`, `marginTop: "var(--space-2)"` -- renders `study.role`
  - Hook: `<p>` with `--text-body`, `--text-secondary`, `marginTop: "var(--space-4)"` -- renders `study.hook`
- The card is NOT clickable (no anchor tag, no cursor:pointer, no hover state) -- per user decision, full case study pages are v2 (DEEP-01). No dead links, no false affordance.
- Server Component, no "use client"

**CaseStudies.tsx (CONT-04):**
- Import `caseStudies` from `@/data/content`, `PageContainer`, and `CaseStudyCard`
- `<section id="work">` with `--bg-secondary` background (alternating: Impact=primary, CaseStudies=secondary)
- Per CONTEXT.md: Case Studies gets a larger H2 (use `--text-h2`)
- Section heading: `<h2>` "Selected Work"
- Per user decision: vertical stack layout, one full-width card per row, editorial feel
- Container: `display: "flex"`, `flexDirection: "column"`, `gap: "var(--space-8)"`
- Map over `caseStudies` array, render one `<CaseStudyCard>` per study
- Server Component, no "use client"
  </action>
  <verify>
    <automated>cd /Users/marieanik/Desktop/portfolio && npx tsc --noEmit src/components/sections/CaseStudyCard.tsx src/components/sections/CaseStudies.tsx 2>&1 | head -20</automated>
  </verify>
  <done>CaseStudyCard renders a non-clickable card with outcome-first title, accent-colored metric, role, and strategic hook. CaseStudies renders 4 cards in a vertical stack inside a section with id="work". Both are Server Components. Both compile without type errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for all 5 component files
2. `npm run build` succeeds
3. Hero uses `<h1>` (the only H1 on the page); all other sections use `<h2>`
4. ImpactMetrics section has `id="impact"`, CaseStudies section has `id="work"`
5. No section component has "use client" directive
6. CaseStudyCard has no anchor tag, no cursor:pointer -- cards are static
7. All content comes from content.ts imports, not hardcoded strings
</verification>

<success_criteria>
- 5 Server Components exist in src/components/sections/
- Hero displays name (H1), role, subtitle with display typography
- What I Do displays 4 categories in responsive grid
- Impact Metrics displays 6 real metrics with accent-colored numbers in 3x2 grid
- Case Studies displays 4 non-clickable cards in vertical stack with real toolkit data
- All components use established inline-style + CSS variable pattern
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-02-SUMMARY.md`
</output>
