---
phase: 02-content-sections
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/sections/BeyondPortfolio.tsx
  - src/components/sections/WhatIStandFor.tsx
  - src/components/sections/TheRightFit.tsx
  - src/components/sections/Contact.tsx
  - src/app/page.tsx
autonomous: false
requirements: [CONT-05, CONT-06, CONT-07, CONT-08]

must_haves:
  truths:
    - "Beyond the Portfolio section displays placeholder content clearly marked for user replacement"
    - "What I Stand For section communicates 4 design values with confident, principled tone"
    - "The Right Fit section names specific roles, team types, and problem spaces Marie targets"
    - "Contact section displays a working mailto link and LinkedIn URL as clear calls to action"
    - "All 8 sections are visible on the page in the correct order: Hero, What I Do, Impact Metrics, Case Studies, Beyond the Portfolio, What I Stand For, The Right Fit, Contact"
    - "Clicking a nav link in the header smooth-scrolls to the correct section"
  artifacts:
    - path: "src/components/sections/BeyondPortfolio.tsx"
      provides: "Personal section with placeholder content"
      contains: "beyondContent"
    - path: "src/components/sections/WhatIStandFor.tsx"
      provides: "Design values section with 4 principles"
      contains: "standForContent"
    - path: "src/components/sections/TheRightFit.tsx"
      provides: "Positioning section with roles, teams, problems"
      contains: "rightFitContent"
    - path: "src/components/sections/Contact.tsx"
      provides: "Contact CTA with email and LinkedIn"
      contains: "contactInfo"
    - path: "src/app/page.tsx"
      provides: "Full page assembly of all 8 sections in order"
      min_lines: 20
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/*"
      via: "imports all 8 section components"
      pattern: "import.*from.*sections"
    - from: "src/components/sections/Contact.tsx"
      to: "src/data/content.ts"
      via: "import { contactInfo }"
      pattern: "mailto:"
    - from: "Header nav links"
      to: "section id attributes"
      via: "href=#id matches section id"
      pattern: "id=\"(work|impact|about|fit|contact)\""
---

<objective>
Build the personal voice sections (Beyond, Stand For, Right Fit) and Contact, then assemble all 8 sections into the final page.

Purpose: Complete the editorial narrative arc with personal positioning sections and a clear conversion endpoint (Contact), then wire everything together in page.tsx so the full portfolio page is visible and navigable.

Output: 4 section components + rewritten page.tsx rendering all 8 sections in order with alternating backgrounds and correct section IDs for navigation.
</objective>

<execution_context>
@/Users/marieanik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marieanik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md
@.planning/phases/02-content-sections/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: content.ts exports -->

From src/data/content.ts:
```typescript
// Personal section content (created in Plan 01)
export const beyondContent: { title: string; body: string };  // placeholder content
export const standForContent: { title: string; values: Array<{ heading: string; description: string }> };
export const rightFitContent: { title: string; summary: string; roles: string[]; teams: string[]; problems: string[]; scale: string };
export const contactInfo: { email: string; linkedinUrl: string; linkedinLabel: string };
```

From src/components/layout/PageContainer.tsx:
```typescript
export function PageContainer({ children, className, as }: PageContainerProps)
```

<!-- From Plan 02: section components -->
From src/components/sections/:
```typescript
export function Hero()           // H1, --bg-primary, no section id
export function WhatIDo()        // H2, --bg-secondary, no section id
export function ImpactMetrics()  // H2, --bg-primary, id="impact"
export function CaseStudies()    // H2, --bg-secondary, id="work"
```

Section order and background alternation (from research):
| # | Section | Background | Section ID | Heading Visual Size |
|---|---------|------------|------------|---------------------|
| 1 | Hero | --bg-primary | (none) | --text-display (H1) |
| 2 | What I Do | --bg-secondary | (none) | --text-h2 (H2) |
| 3 | Impact Metrics | --bg-primary | impact | --text-h2 (H2) |
| 4 | Case Studies | --bg-secondary | work | --text-h2 (H2) |
| 5 | Beyond the Portfolio | --bg-primary | about | --text-h3 visual (H2 semantic) |
| 6 | What I Stand For | --bg-secondary | (none) | --text-h3 visual (H2 semantic) |
| 7 | The Right Fit | --bg-primary | fit | --text-h3 visual (H2 semantic) |
| 8 | Contact | --bg-secondary | contact | --text-h2 (H2) |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BeyondPortfolio, WhatIStandFor, TheRightFit, and Contact section components</name>
  <files>src/components/sections/BeyondPortfolio.tsx, src/components/sections/WhatIStandFor.tsx, src/components/sections/TheRightFit.tsx, src/components/sections/Contact.tsx</files>
  <action>
All four components follow the established Server Component pattern. No "use client" directive. Import `PageContainer` from `@/components/layout/PageContainer` and relevant content from `@/data/content`.

Per CONTEXT.md locked decisions: personal sections use varied heading visual sizes (smaller --text-h3 styling) but remain semantic `<h2>`. Tone varies by section.

**BeyondPortfolio.tsx (CONT-05):**
- Import `beyondContent` from `@/data/content`
- `<section id="about">` with `--bg-primary` background (section 5, alternating from CaseStudies=secondary)
- `<h2>` with visual styling using `--text-h3` (smaller heading per CONTEXT.md hierarchy), `--text-primary`, `--font-semibold` -- render beyondContent.title ("Beyond the Portfolio")
- Body text with `--text-body`, `--text-secondary` -- render beyondContent.body (placeholder text)
- Keep layout simple -- this section will be short text content once Marie provides it

**WhatIStandFor.tsx (CONT-06):**
- Import `standForContent` from `@/data/content`
- `<section>` with `--bg-secondary` background (section 6). No section id needed -- covered by #about on BeyondPortfolio
- `<h2>` with visual `--text-h3` styling, render standForContent.title
- Render standForContent.values as a list of principles. For each value:
  - `<h3>` with `--text-h3` (or --text-body with --font-semibold), `--text-primary` for the heading
  - `<p>` with `--text-body`, `--text-secondary` for the description
- Layout: vertical stack or 2-column grid on desktop. Vertical flex with `gap: "var(--space-8)"` is simplest and editorial
- Tone: confident and principled (per CONTEXT.md)

**TheRightFit.tsx (CONT-07):**
- Import `rightFitContent` from `@/data/content`
- `<section id="fit">` with `--bg-primary` background (section 7)
- `<h2>` with visual `--text-h3` styling, render rightFitContent.title
- Opening paragraph: `--text-lg`, `--text-secondary`, render rightFitContent.summary
- Display structured positioning data in labeled groups. For each category (roles, teams, problems):
  - Label with `--text-sm`, `--text-secondary`, `--font-semibold`, uppercase or small-caps
  - Items as inline text or comma-separated list with `--text-body`, `--text-primary`
- Scale statement at the bottom with `--text-body`, `--text-secondary`
- Tone: direct and clear (per CONTEXT.md)

**Contact.tsx (CONT-08):**
- Import `contactInfo` from `@/data/content`
- `<section id="contact">` with `--bg-secondary` background (section 8, last section)
- `<h2>` with `--text-h2` styling (Contact gets standard heading size as the conversion endpoint)
- Brief CTA text: "Currently open to senior product design roles where I can drive strategic impact through research-informed design." (or import from content.ts if defined there) -- `--text-lg`, `--text-secondary`
- Two CTAs side by side with flex layout, `gap: "var(--space-4)"`:
  1. Email CTA: `<a href="mailto:{contactInfo.email}">` styled as primary button (same pattern as existing page.tsx accent button): `--accent-primary` background, `--text-on-accent` color, `--radius-md`, `--font-semibold`, `--glow-accent` shadow, padding `--space-3` / `--space-8`
  2. LinkedIn CTA: `<a href="{contactInfo.linkedinUrl}" target="_blank" rel="noopener noreferrer">` styled as secondary/outline button: transparent background, `border: "2px solid var(--accent-primary)"`, `--accent-primary` text color, same padding and border-radius
- Both links are regular `<a>` tags (NOT Next.js Link -- external URL and mailto don't benefit from client routing)
  </action>
  <verify>
    <automated>cd /Users/marieanik/Desktop/portfolio && npx tsc --noEmit src/components/sections/BeyondPortfolio.tsx src/components/sections/WhatIStandFor.tsx src/components/sections/TheRightFit.tsx src/components/sections/Contact.tsx 2>&1 | head -20</automated>
  </verify>
  <done>BeyondPortfolio renders placeholder content with clear TODO marker. WhatIStandFor renders 4 design values. TheRightFit renders roles/teams/problems positioning. Contact renders mailto and LinkedIn CTAs. All are Server Components without type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite page.tsx to assemble all 8 sections in editorial order</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the entire contents of `src/app/page.tsx` with a clean assembly of all 8 section components.

Import Header from `@/components/layout/Header` and all 8 section components from `@/components/sections/`:
- Hero, WhatIDo, ImpactMetrics, CaseStudies, BeyondPortfolio, WhatIStandFor, TheRightFit, Contact

Structure:
```tsx
export default function Home() {
  return (
    <>
      <Header />
      <main style={{ paddingTop: "var(--header-height)" }}>
        <Hero />
        <WhatIDo />
        <ImpactMetrics />
        <CaseStudies />
        <BeyondPortfolio />
        <WhatIStandFor />
        <TheRightFit />
        <Contact />
      </main>
    </>
  );
}
```

This is intentionally minimal. Each section component handles its own:
- Background color (alternating --bg-primary / --bg-secondary)
- Section padding (--space-section)
- Section id for navigation
- PageContainer wrapping
- Content rendering

The page component is just orchestration -- it defines the order.

Do NOT add any inline styles, state, or logic to page.tsx beyond the `paddingTop` on `<main>` (to offset the sticky header).
  </action>
  <verify>
    <automated>cd /Users/marieanik/Desktop/portfolio && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>page.tsx imports and renders all 8 sections in correct order. The full build succeeds. The page renders a complete editorial portfolio with Hero, What I Do, Impact Metrics, Case Studies, Beyond the Portfolio, What I Stand For, The Right Fit, and Contact sections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete portfolio page with all 8 sections and smooth-scroll navigation</name>
  <files>n/a</files>
  <action>
Human verification of the complete 8-section portfolio page. The executor should start the dev server (`npm run dev`) before presenting this checkpoint.
  </action>
  <verify>
    <automated>cd /Users/marieanik/Desktop/portfolio && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually verified all 8 sections render correctly, nav links smooth-scroll to correct positions, responsive layout works, and both theme modes display properly.</done>
  <what-built>Complete 8-section portfolio page with real content, smooth-scroll navigation, and editorial layout</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000 in your browser
    2. Verify all 8 sections are visible by scrolling down the page:
       - Hero (Marie Anik Paradis, Senior UX and Product Designer)
       - What I Do (4 specialty categories)
       - Impact Metrics (6 large accent-colored numbers)
       - Case Studies (4 cards: EPIC.submit, EPIC.engage, NSC Interactions, Bambora)
       - Beyond the Portfolio (placeholder content)
       - What I Stand For (4 design values)
       - The Right Fit (roles, teams, problems)
       - Contact (email and LinkedIn buttons)
    3. Click each nav link in the header (Work, Impact, About, Fit, Contact) and verify smooth scrolling to the correct section with the heading visible (not hidden behind the header)
    4. Resize the browser to mobile width and verify sections stack responsively (metrics grid collapses, nav links hidden)
    5. Add ?mode=dark to the URL and verify all sections look correct in dark mode (accent colors switch to cyan, backgrounds switch to dark)
    6. Verify case study cards are NOT clickable (no pointer cursor, no link behavior)
    7. Check that "Beyond the Portfolio" clearly shows placeholder text
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds without errors
2. All 8 sections render in correct order on the page
3. Section IDs match nav link hrefs: impact, work, about, fit, contact
4. Smooth scroll navigates to correct sections with proper header offset
5. All sections are responsive (mobile, tablet, desktop)
6. Light and dark modes both render correctly
7. Only one `<h1>` on the page (in Hero)
8. All other headings are semantic `<h2>` (visual size varies via CSS)
9. No "use client" directive on any section component
</verification>

<success_criteria>
- All 8 sections visible on the page in the correct editorial order
- Nav links smooth-scroll to correct sections
- Case study cards display real toolkit data (not placeholder)
- Contact section has working mailto and LinkedIn links
- Personal sections have appropriate content (placeholder for Beyond, drafted for Stand For and Right Fit)
- Responsive layout works across breakpoints
- Both theme modes render correctly
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-03-SUMMARY.md`
</output>
